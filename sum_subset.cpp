#include <iostream>

using namespace std;

int n, k;
const int N = 1e5 + 10;
int a[N];

int[] get_res()
{
    a[0] = 0;
    int ls_bigger[];
    for (int i = 0; i < n; i++)  
}

int main()
{
    cin << n << k;
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &a[i]);
    }

    int res[] = get_res();
    for (auto a : res) cout << a << endl;
    return 0;
}
